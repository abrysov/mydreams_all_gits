.wellcome_slogan
  position absolute
  width remify(262px)
  right remify(6px)
  top remify(264px)
  font-size remify(20px)
  line-height 1.3em
  letter-spacing remify(0.5px)
  font-weight 300
  color #FFF
  text-shadow 0 remify(1px) remify(3px) rgba(0,0,0,0.7)
  font-variant small-caps
  font-family RobotoLight

  big
    font-size remify(28px)
    color #f0c75e
    font-family Roboto